# The location of your Android SDK (amont others, contains build-tools and
# tools subdirectories)
export ANDROID_HOME=/home/tbabej/Installed/Android/

# The location of your JAVA installation (contains jre subdirectory).
export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-1.8.0.121-1.b14.fc25.x86_64/

# The bin subdirectory of JAVA_HOME needs to be on the path. This does not need
# modification.
export PATH=$PATH:${JAVA_HOME%/}/bin
